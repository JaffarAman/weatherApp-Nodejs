<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <div class="container">
      <header>
        <div class="logo">
          <img src="/img/Compass-pass-clip-art-5.png" alt="" />
        </div>
      </header>
      <div class="content_heading">
        <h1>Simply Weather</h1>
        <p>View the current Weather for your city</p>
      </div>
      <form action="/" class="formBox" method="POST">
        <input type="text" placeholder="Enter a City" name="city" />
      </form>
      <div class="weatherDetails">
        <% if(temp !== null){%>
        <h1><%= temp%></h1>
        <%}%>
        <div class="icon">
          <% if(icon !== null){%> <img src="http://openweathermap.org/img/w/<%=
          icon %>.png" alt=''" /> <%}%> <% if(des !== null){%>
          <h1><%= des%></h1>
          <%}%>
        </div>
        <!-- <h3>Karachi</h3> -->
        <% if(city !== null){%>
        <h1><%= city%></h1>
        <%}%>
      </div>
    </div>
  </body>
</html>
